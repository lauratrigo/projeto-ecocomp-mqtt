As views do projeto estao nos arquivos HTML, CSS e JS da raiz para preservar os caminhos ja utilizados pelo frontend.

Esta pasta existe para representar a camada View na estrutura em camadas solicitada:

Router -> Middleware -> Controller -> Service -> DAO -> Model
